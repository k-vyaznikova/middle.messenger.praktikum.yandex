    <div class = "contacts">
        <div class = "fixed-container search-block">
            <div class = "fixed-block">      
                {{{Search submit_url = search.submit_url not_focused="yes"}}}
            </div>
        </div>  
        <div class = "contact-list">
            {{#if is_loaded}}
                {{#each contacts}}
                    {{{ContactItem contactSelected = contactSelected contactDialogLink = contactDialogLink contactImg = contactImg contactName = contactName yourMsg = yourMsg contactMsg = contactMsg contactTimeMsg = contactTimeMsg contactQMsg = contactQMsg onClick = onClick ref=ref}}}
                {{/each}}
            {{else}}
                Идет загрузка...
            {{/if}}
        </div>
    </div>
