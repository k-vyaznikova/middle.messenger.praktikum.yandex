    {{#if edit_mode}}
        <form class = "profile-form">
        {{{errorMsg}}} 
        
    {{else}}
        <div  class = "profile-form">
    {{/if}}    
        {{{profilePhoto}}}
        <div class = "profile-header">{{title}}</div>
        <div class = "profile-body password-edit">
            <div class="success-msg">{{success_msg}}</div>
            {{#each profile_items}}
                {{{this}}}
            {{/each}}
        </div>
       
            {{#if edit_mode}}
                <div class = "profile-footer form-footer">
                {{{submitBtn}}}
                </div>
            {{/if}}    
            {{#if footer_links}}
                <div class = "profile-footer">
                    <div class = "action-link">{{{link_1}}}</div>
                    <div class = "action-link">{{{link_2}}}</div>
                    <div class = "action-link">{{{logoutBtn}}}</div>
                </div>
            {{/if}}  
    {{#if edit_mode}}             
        </form>
    {{else}}
        </div>
    {{/if}}        
